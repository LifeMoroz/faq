{% extends "base.html" %}

{% block content %}
<div class="panel panel-info panel-default" >
<div class="panel-heading"><h3> User {{ question_user.username }}</h3> 
  </div>
</div>
{% if question_user.questions.all %}
<h3>Questions from {{ question_user.username }}:</h3>
{% for question in question_user.questions.all %}
<div class="panel panel-default">
  <div class="panel-body">
      <div class="row">
      <div class="col-xs-6"><a href="{{ question.get_absolute_url }}"><h5>{{ question.title }}</h5></a></div>
      </div>
  </div>
</div>
{% endfor %}
{% else %}
<h3>This user is too clever to ask questions</h3>
{% endif %}

{% if question_user.answers.all %}
<h3>Answers:</h3>
{% for answer in question_user.answers.all %}
<div class="panel panel-default">
  <div class="panel-body">
      <div class="row">
      <div class="col-xs-6"><a href="{{ answer.get_absolute_url }}"><h5>{{ answer.content }}</h5></a></div>
      </div>
  </div>
</div>
{% endfor %}
{% else %}
<h3>This user never answer questions</h3>
{% endif %}


{% endblock %}
